# Скрипт для сбора данных о малом бизнесе в Праге

## Описание

Скрипт `prague_small_businesses.py` собирает информацию о малом бизнесе в Праге и экспортирует её в Excel файл.

## Что собирает скрипт

- **Название бизнеса**
- **Телефон** (нормализованный чешский формат)
- **Email**
- **Имя владельца**
- **Фамилия владельца**
- **Полное имя владельца**
- **Адрес**
- **Район Праги**
- **Веб-сайт**
- **Категория бизнеса**

## Требования

1. **Google Maps API ключ** - должен быть установлен в переменной окружения `GOOGLE_MAPS_API_KEY` или в файле `.env`
2. **Python пакеты**:
   - pandas
   - openpyxl
   - pydantic
   - httpx
   - playwright (опционально, для поиска информации о владельцах на сайтах)

## Установка зависимостей

```bash
pip install pandas openpyxl pydantic httpx playwright
```

Если хотите искать владельцев на сайтах:
```bash
playwright install chromium
```

## Использование

### Базовое использование (все категории, все районы)

```bash
python scripts/prague_small_businesses.py
```

### С указанием категорий

```bash
python scripts/prague_small_businesses.py --categories kavárna kadeřnictví kosmetika restaurace
```

### С ограничением количества результатов

```bash
python scripts/prague_small_businesses.py --max-per-category 50
```

### С указанием конкретных районов

```bash
python scripts/prague_small_businesses.py --districts "Prague 1" "Prague 2" "Prague 3"
```

### С указанием выходного файла

```bash
python scripts/prague_small_businesses.py --output-file "prague_businesses.xlsx"
```

### Полный пример

```bash
python scripts/prague_small_businesses.py \
  --categories kavárna kadeřnictví kosmetika \
  --max-per-category 30 \
  --districts "Prague 1" "Prague 2" \
  --output-dir "leads" \
  --output-file "prague_small_businesses.xlsx"
```

## Параметры командной строки

- `--categories` - Список категорий для поиска (по умолчанию: все основные категории)
- `--max-per-category` - Максимум результатов на категорию (по умолчанию: 30)
- `--districts` - Районы Праги для поиска (по умолчанию: все районы 1-10)
- `--output-dir` - Директория для сохранения результатов (по умолчанию: "leads")
- `--output-file` - Имя Excel файла (автогенерируется если не указано)
- `--api-key` - Google Maps API ключ (опционально, берется из env если не указан)

## Категории бизнеса

По умолчанию скрипт ищет следующие категории:
- Кафе и рестораны: kavárna, restaurace, bistro, café
- Салоны красоты: kadeřnictví, kosmetika, manikúra, pedikúra, salon krásy
- Розничная торговля: obchod, obchůdek, prodejna, butik
- Услуги: úklid, opravy, servis, instalatér, elektrikář
- Медицина: lékař, zubní lékař, fyzioterapeut, masáž
- Фитнес: fitness, posilovna, yoga, pilates, sportovní centrum

## Районы Праги

По умолчанию скрипт ищет во всех районах:
- Prague 1, Prague 2, Prague 3, Prague 4, Prague 5
- Prague 6, Prague 7, Prague 8, Prague 9, Prague 10

## Результаты

Скрипт создает Excel файл в указанной директории с именем вида:
`prague_small_businesses_YYYYMMDD_HHMMSS.xlsx`

Файл содержит лист "Малый бизнес Праги" с колонками:
- Название бизнеса
- Телефон
- Email
- Имя владельца
- Фамилия владельца
- Полное имя владельца
- Адрес
- Район
- Веб-сайт
- Категория

## Примечания

1. Скрипт использует Google Maps Places API для поиска бизнесов
2. Поиск информации о владельцах выполняется путем скрапинга веб-сайтов (требует Playwright)
3. Скрипт автоматически нормализует чешские телефонные номера в формат +420 XXX XXX XXX
4. Скрипт применяет rate limiting для соблюдения лимитов API
5. Логи сохраняются в директории `logs/`

## Пример вывода

```
============================================================
НАЧАЛО СБОРА ДАННЫХ О МАЛОМ БИЗНЕСЕ В ПРАГЕ
============================================================

[1/20] Поиск: kavárna Prague 1...
  ✓ Café Louvre (Владелец: не найден, Телефон: ✓, Email: ✗)
  ✓ Kavárna Slavia (Владелец: Jan Novák, Телефон: ✓, Email: ✓)
...

============================================================
ОБРАБОТКА ЗАВЕРШЕНА
============================================================

✓ Результаты сохранены в: leads/prague_small_businesses_20241201_143022.xlsx

📊 СТАТИСТИКА:
  Всего найдено бизнесов: 150
  С телефоном: 120 (80%)
  С email: 85 (57%)
  С именем владельца: 45 (30%)
```
