# Анализ архитектуры проекта: Поиск "Богов" и проблем

## 🔴 Критические проблемы (God Objects)

### 1. **BusinessCollector** - Главный "Бог" ⚠️

**Проблемы:**

- **Множественные ответственности**: Управляет 4 разными клиентами (API, HERE, MCP, Scraper)
- **Нарушение SRP**: Объединяет в себе:
  - Координацию источников данных (fallback логика)
  - Управление параллелизмом (семафоры, батчинг)
  - Сохранение в БД
  - Трекинг прогресса
  - Обработку ошибок
- **Tight Coupling**: Прямые зависимости от всех клиентов и БД
- **Сложность тестирования**: Невозможно протестировать изолированно

**Размер:**

- 327 строк кода
- 7 методов
- 4 прямые зависимости от клиентов
- 1 зависимость от БД (lazy import)

**Рекомендации:**

```python
# Разделить на:
1. DataSourceCoordinator - координация источников с fallback
2. QueryProcessor - обработка запросов и батчинг
3. BusinessRepository - сохранение в БД (уже есть BusinessExporter, но он для экспорта)
4. ProgressTracker - трекинг прогресса (уже есть CollectionProgress, но он dataclass)
```

---

### 2. **GoogleMapsScraper** - Второй "Бог" ⚠️

**Проблемы:**

- **Множественные ответственности**:
  - Управление браузером (Playwright)
  - Парсинг HTML/DOM
  - Извлечение контактов с веб-сайтов
  - Rate limiting
  - Навигация и скроллинг
- **Большой размер**: ~494 строки кода
- **Много методов**: ~10+ методов
- **Смешанные уровни абстракции**: Низкоуровневая работа с DOM + высокоуровневая бизнес-логика

**Рекомендации:**

```python
# Разделить на:
1. BrowserManager - управление браузером и страницами
2. GoogleMapsParser - парсинг результатов Google Maps
3. ContactExtractor - извлечение контактов (можно вынести в отдельный модуль)
4. RateLimiter - rate limiting (можно вынести в утилиту)
```

---

## 🟡 Средние проблемы (Coupling & Complexity)

### 3. **BusinessExporter** - Tight Coupling

**Проблемы:**

- Прямая зависимость от БД клиента через lazy import
- Нарушает Dependency Inversion Principle
- Сложно тестировать без реальной БД

**Рекомендации:**

```python
# Использовать Dependency Injection:
class BusinessExporter:
    def __init__(self, db_client: Optional[DatabaseClient] = None):
        self.db_client = db_client or self._get_db_client()
```

---

### 4. **MCPGoogleMapsClient** - Избыточная обертка

**Проблемы:**

- Fallback на `GoogleMapsAPIClient` создает coupling
- Дублирует функциональность API клиента
- Неясная ответственность: MCP или API fallback?

**Рекомендации:**

- Убрать fallback логику из MCP клиента
- Использовать Strategy pattern в BusinessCollector для выбора источника

---

### 5. **GoogleMapsClient** - Избыточная обертка

**Проблемы:**

- Обертка над другими клиентами без добавления ценности
- Используется только в старых скриптах (`prague_business_leads.py`, `french_business_leads.py`)
- Дублирует функциональность `BusinessCollector`

**Рекомендации:**

- Рассмотреть удаление, если не используется активно
- Или переписать старые скрипты на использование `BusinessCollector`

---

## 🟢 Мелкие проблемы (Code Smells)

### 6. **Множественные импорты в collector.py**

**Проблемы:**

- Lazy imports внутри методов (`from .config import ScraperConfig` в `__init__` и `_fetch_businesses`)
- Lazy import БД клиента (хорошо для избежания circular imports, но можно улучшить)

**Рекомендации:**

- Вынести все импорты наверх файла
- Использовать dependency injection для БД клиента

---

### 7. **Дублирование логики преобразования location**

**Проблемы:**

- В `BusinessCollector._fetch_businesses` повторяется логика преобразования `location` (dict/tuple) для каждого клиента
- Код дублируется 3 раза (API, HERE, MCP)

**Рекомендации:**

```python
# Вынести в утилиту:
def normalize_location(location: Union[Dict, Tuple, str]) -> Dict[str, float]:
    """Normalize location to dict format."""
    ...
```

---

### 8. **Hardcoded значения в utils.py**

**Проблемы:**

- `PRAGUE_DISTRICTS` и `BUSINESS_CATEGORIES` захардкожены в коде
- Сложно расширять без изменения кода

**Рекомендации:**

- Вынести в конфигурационный файл (JSON/YAML)
- Или сделать загружаемыми из БД/API

---

## 📊 Метрики сложности

### Размер классов (строк кода)

**После рефакторинга:**

1. `GoogleMapsScraper`: ~494 строки ⚠️ (требует дальнейшего разделения)
2. `BusinessCollector`: ~148 строк ✅ (было 327, уменьшение на 55%)
3. `DataSourceCoordinator`: ~163 строки ✅ (новый компонент)
4. `BusinessRepository`: ~88 строк ✅ (новый компонент)
5. `ContactExtractor`: ~75 строк ✅ (новый компонент)
6. `GoogleMapsAPIClient`: ~413 строк (но хорошо структурирован)
7. `HerePlacesClient`: ~245 строк ✅
8. `MCPGoogleMapsClient`: ~223 строки ✅

### Количество зависимостей

- `BusinessCollector`: 4 клиента + БД + конфиг
- `GoogleMapsScraper`: Playwright + stealth + contact extractor + config + models
- `BusinessExporter`: БД клиент

---

## 🎯 Приоритеты рефакторинга

### ✅ Высокий приоритет (ВЫПОЛНЕНО)

1. ✅ **Разделить BusinessCollector** на отдельные классы
   - ✅ Создан `DataSourceCoordinator` - координация источников с fallback
   - ✅ Создан `BusinessRepository` - сохранение в БД
   - ✅ Рефакторен `BusinessCollector` - теперь только батчинг и параллелизм
   - ✅ Уменьшение кода с 327 до 148 строк (-55%)

2. ✅ **Создан ContactExtractor** - отдельный модуль для извлечения контактов
   - ✅ Вынесен из GoogleMapsScraper
   - ✅ Поддержка библиотеки и fallback

3. ✅ **Вынесена normalize_location** в utils.py
   - ✅ Устранено дублирование (было 3 копии)

### Средний приоритет

1. Разделить GoogleMapsScraper на BrowserManager и Parser
   - BrowserManager - управление браузером
   - GoogleMapsParser - парсинг результатов
   - RateLimiter - вынести в утилиту

2. Убрать fallback логику из MCPGoogleMapsClient
3. Использовать Dependency Injection в BusinessExporter

### Низкий приоритет

1. Рассмотреть удаление GoogleMapsClient
2. Вынести константы в конфигурацию

---

## ✅ Что сделано хорошо

1. **Модели данных** (`models.py`) - хорошо структурированы, используют Pydantic
2. **Конфигурация** (`config.py`) - централизованная, с загрузкой из env
3. **Утилиты** (`utils.py`) - чистые функции, легко тестируемые
4. **Разделение клиентов** - каждый источник данных в отдельном классе
5. **Использование async/await** - правильная асинхронная архитектура

---

## 📝 Рекомендации по рефакторингу

### Шаг 1: Создать интерфейсы/протоколы

```python
from typing import Protocol

class DataSource(Protocol):
    async def search_businesses(
        self, query: str, location: Dict, max_results: int
    ) -> List[BusinessData]:
        ...
```

### Шаг 2: Разделить BusinessCollector

```python
class DataSourceCoordinator:
    """Координирует источники данных с fallback."""

class QueryProcessor:
    """Обрабатывает запросы с батчингом и параллелизмом."""

class BusinessRepository:
    """Сохраняет бизнесы в БД."""
```

### Шаг 3: Разделить GoogleMapsScraper

```python
class BrowserManager:
    """Управляет браузером и страницами."""

class GoogleMapsParser:
    """Парсит результаты Google Maps."""

class ContactExtractor:
    """Извлекает контакты с веб-сайтов."""
```

---

**Дата анализа**: Январь 2025
**Версия проекта**: После очистки от старых файлов
