# О Проекте — Telegram Бот для Записи в Салон Красоты

Production-ready Telegram бот для записи на прием в салон красоты с интеграцией платежей, напоминаний и AI-поддержкой клиентов.

## 📋 Описание

Этот проект представляет собой полнофункциональный MVP Telegram-бота для управления записями в салон красоты. Бот позволяет клиентам:

- Выбирать услуги из каталога
- Просматривать доступные временные слоты
- Бронировать время приема
- Оплачивать услуги через Stripe
- Получать автоматические напоминания о записи
- Получать ответы на вопросы через AI (Claude)

## 🏗️ Архитектура

Проект построен на принципах чистой архитектуры с четким разделением ответственности:

```
telegram-beauty-salon-bot/
├── bot/                    # Обработчики бота и FSM состояния
│   ├── handlers.py         # Основные обработчики сообщений/колбэков
│   ├── keyboards.py        # Построители inline клавиатур
│   └── states.py           # Определения состояний FSM
├── db/                     # Слой работы с БД
│   └── supabase_client.py  # CRUD операции с Supabase
├── models/                 # Pydantic модели
│   ├── booking.py          # Модели бронирований
│   ├── client.py           # Модели клиентов
│   ├── slot.py             # Модели временных слотов
│   └── service.py          # Определения услуг
├── payments/               # Обработка платежей
│   └── stripe.py           # Интеграция со Stripe
├── scheduler/              # Планирование задач
│   └── reminders.py       # Напоминания о записях
├── utils/                  # Утилиты
│   ├── ai_qa.py           # Интеграция с Claude AI
│   └── logging_config.py  # Конфигурация логирования
├── agents/                # Параллельные агенты для разработки
│   ├── base_agent.py      # Базовый класс агента
│   ├── architect.py       # Агент архитектора
│   ├── coder_bot.py       # Агент разработчика бота
│   ├── coder_db.py        # Агент разработчика БД
│   ├── tester.py          # Агент тестирования
│   ├── reviewer.py        # Агент ревьюера
│   └── ...                # Другие специализированные агенты
├── tests/                 # Тесты
│   ├── test_basic.py      # Базовые unit-тесты
│   └── test_e2e.py        # E2E тесты (Playwright)
├── scripts/               # Скрипты автоматизации
│   ├── auto_test_deploy.py # Автоматический тест и деплой
│   ├── mcp_github_deploy.py # Деплой через MCP GitHub
│   └── setup_db.py        # Настройка БД
├── bot.py                 # Точка входа
├── config.py              # Управление конфигурацией
├── webhook.py             # Обработчик Stripe webhooks
├── requirements.txt       # Python зависимости
├── Dockerfile             # Определение контейнера
└── docker-compose.yml     # Docker Compose конфигурация
```

### Принципы архитектуры

1. **Разделение ответственности**: Каждый модуль отвечает за свою область
2. **Типобезопасность**: Использование Pydantic для валидации данных
3. **Асинхронность**: Полная поддержка async/await с aiogram 3.x
4. **Обработка ошибок**: Комплексная обработка ошибок и логирование
5. **Production-ready**: Docker, CI/CD, логирование, мониторинг

## 🚀 Основные возможности

### ✅ Основной функционал

- **Бронирование услуг**: Выбор из 4 услуг (Маникюр, Стрижка, Педикюр, Чистка лица)
- **Календарь слотов**: Просмотр и бронирование доступных временных слотов
- **Платежи Stripe**: Безопасная обработка платежей
- **Напоминания**: Автоматические напоминания за 24 часа до записи через Telegram
- **AI Q&A**: Поддержка клиентов через Claude AI
- **GDPR Compliance**: Экран согласия и защита данных

### 🔄 Процесс бронирования

1. Пользователь отправляет `/start`
2. Экран согласия GDPR (при первом использовании)
3. Главное меню с опциями
4. Выбор услуги → Выбор слота → Подтверждение → Оплата
5. Получение подтверждения и напоминания

### 💳 Процесс оплаты

1. Создание бронирования со статусом `pending`
2. Создание Stripe PaymentIntent
3. Перенаправление пользователя на Stripe Checkout
4. Webhook обновляет бронирование на `paid` при успехе
5. Слот помечается как `booked`

### 🔔 Система напоминаний

- APScheduler запускается каждый час
- Проверяет бронирования за 24 часа до приема
- Отправляет напоминание через Telegram
- Помечает напоминание как отправленное

## 🗄️ База данных

### Схема (ERD)

См. [ERD.md](ERD.md) для подробной диаграммы связей сущностей.

**Сущности:**

- `clients` — Информация о клиентах
- `slots` — Доступные временные слоты
- `bookings` — Бронирования

**Связи:**

- `clients` 1:N `bookings` (один клиент может иметь много бронирований)
- `slots` 1:N `bookings` (один слот может иметь одно бронирование)

### Бизнес-правила

1. **Доступность слотов**: Слот может быть забронирован только если `status = 'available'`
2. **Предотвращение двойного бронирования**: При создании бронирования статус слота меняется на `booked`
3. **Поток оплаты**: Бронирование начинается как `pending`, становится `paid` после подтверждения Stripe
4. **Напоминания**: Отправляются только для `confirmed` или `paid` бронирований, которые еще не получили напоминание
5. **GDPR**: Клиент должен дать согласие перед созданием любого бронирования

## 🤖 Параллельные агенты и автоматизация

### Интеграция с Composer

Использование Cursor Composer (Ctrl+K) с параллельными агентами:

**Быстрые промпты (Vibe Mode):**

```
@agent1 Architect plan
@agent2 Code handlers
@tester Run Playwright
```

**Параллельный запуск:**

```
Spawn 5 agents: 2 coders (bot/db), 1 tester, 1 devops (docker), 1 reviewer.
```

**Итеративные исправления:**

```text
Fix bug: overbooking → Apply → @web search GDPR Telegram
```

См. [COMPOSER_PROMPTS.md](COMPOSER_PROMPTS.md) для подробных промптов.

### TMUX агенты (20 терминалов)

**Версия для Windows:**

```bash
# Настройка 20 параллельных процессов агентов
py tmux_agents_parallel_windows.py setup

# Список активных агентов
py tmux_agents_parallel_windows.py list

# Проверка здоровья агентов
py tmux_agents_parallel_windows.py health

# Остановка всех агентов
py tmux_agents_parallel_windows.py stop
```

**Linux/macOS (с tmux):**

```bash
# Настройка 20 параллельных терминалов агентов
python tmux_agents_parallel.py setup

# Список активных агентов
python tmux_agents_parallel.py list

# Подключение к сессии
tmux attach -t agents
```

Каждый агент выполняет определенную задачу и логирует в `logs/agent_<name>.log`.

### Автоматический тест и деплой

```bash
# Запуск полного пайплайна: тест → сборка → деплой
python scripts/auto_test_deploy.py

# Или использование GitHub Actions (автоматически при push)
# См. .github/workflows/auto_deploy.yml
```

**Шаги пайплайна:**

1. ✅ Линтер (ruff)
2. ✅ Тесты (pytest + Playwright)
3. ✅ Docker сборка
4. ✅ Деплой (Docker/Vercel)
5. ✅ Создание PR (через MCP GitHub)
6. ✅ Деплой на Vercel (через MCP)

### Интеграция MCP GitHub

Настройка MCP GitHub сервера в `.kilocode/mcp.json`:

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "your_token_here"
      }
    }
  }
}
```

**Использование:**

- Создание PR: `MCP GitHub: Create pull request`
- Деплой: `MCP GitHub: Trigger Vercel deployment`

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `BOT_TOKEN` | Токен Telegram бота | ✅ |
| `SUPABASE_URL` | URL проекта Supabase | ✅ |
| `SUPABASE_KEY` | Анонимный ключ Supabase | ✅ |
| `STRIPE_SECRET_KEY` | Секретный ключ Stripe | ✅ |
| `STRIPE_PUBLISHABLE_KEY` | Публикуемый ключ Stripe | ✅ |
| `STRIPE_WEBHOOK_SECRET` | Секрет для верификации webhook | ❌ |
| `CLAUDE_API_KEY` | API ключ Claude | ✅ |
| `TIMEZONE` | Часовой пояс (по умолчанию: Europe/Prague) | ❌ |
| `REMINDER_HOURS_BEFORE` | Часов до напоминания (по умолчанию: 24) | ❌ |
| `HOST` | Хост сервера (по умолчанию: 0.0.0.0) | ❌ |
| `PORT` | Порт сервера (по умолчанию: 8000) | ❌ |

### Услуги и цены

- **Маникюр**: 150 CZK, 60 минут
- **Стрижка и укладка**: 200 CZK, 90 минут
- **Педикюр**: 180 CZK, 75 минут
- **Чистка лица**: 250 CZK, 60 минут

## 🚢 Развертывание

### Быстрый старт

1. **Клонирование и настройка:**

```bash
git clone <repository-url>
cd telegram-beauty-salon-bot
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

1. **Настройка окружения:**

```bash
cp .env.example .env
# Отредактируйте .env с вашими учетными данными
```

1. **Настройка базы данных Supabase:**

Выполните SQL команды из README.md в Supabase SQL Editor.

1. **Запуск бота:**

```bash
python bot.py
```

### Docker развертывание

```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot

# Остановка
docker-compose down
```

### GitHub Actions CI/CD

Проект включает GitHub Actions workflow (`.github/workflows/deploy.yml`), который:

1. Запускает тесты и линтер
2. Собирает Docker образ
3. Деплоит на ваш сервер (настройте SSH/Vercel)

### Ручной деплой

1. **Сборка Docker образа:**

```bash
docker build -t telegram-beauty-salon-bot .
```

1. **Запуск контейнера:**

```bash
docker run -d \
  --name beauty-salon-bot \
  --env-file .env \
  telegram-beauty-salon-bot
```

1. **Настройка Stripe Webhook:**
   - URL: `https://your-domain.com/webhook/stripe`
   - События: `payment_intent.succeeded`, `payment_intent.payment_failed`

## 🧪 Тестирование

```bash
# Установка зависимостей для тестов
pip install pytest pytest-asyncio playwright

# Установка браузеров Playwright
playwright install

# Запуск тестов
pytest tests/ -v
```

## 📝 Обработанные граничные случаи

- ✅ **Предотвращение двойного бронирования**: Статус слота проверяется перед бронированием
- ✅ **Ошибки оплаты**: Бронирование может быть отменено при ошибке оплаты
- ✅ **Неявки**: Отслеживание статуса для завершенных/отмененных бронирований
- ✅ **Конфликты слотов**: Ограничения БД предотвращают перебронирование
- ✅ **GDPR соответствие**: Согласие требуется перед обработкой данных

## 🔒 Безопасность

- ✅ Переменные окружения для секретов
- ✅ Валидация входных данных с Pydantic
- ✅ Верификация подписи Stripe webhook (реализовать в production)
- ✅ GDPR-совместимая обработка данных
- ✅ Нет захардкоженных учетных данных

## 📚 API Документация

### Команды бота

- `/start` — Запуск бота и показ главного меню

### Callback данные

- `book_appointment` — Начало процесса бронирования
- `service_{type}` — Выбор услуги
- `slot_{id}_{type}` — Выбор временного слота
- `confirm_booking_{id}` — Подтверждение бронирования
- `payment_done_{id}` — Отметка оплаты как завершенной
- `my_bookings` — Просмотр бронирований пользователя
- `ask_question` — Начало AI Q&A

## 🛠️ Разработка

### Структура проекта

- **Многофайловая архитектура**: Четкое разделение ответственности
- **Типобезопасность**: Pydantic модели для валидации
- **Async/await**: Полная поддержка async с aiogram 3.x
- **Обработка ошибок**: Комплексная обработка ошибок и логирование
- **Production-ready**: Docker, CI/CD, логирование, мониторинг

### Добавление новых функций

1. **Новая услуга:**
   - Добавить в `models/service.py` словарь `SERVICES`
   - Обновить слоты в базе данных

2. **Новый обработчик:**
   - Добавить функцию обработчика в `bot/handlers.py`
   - Зарегистрировать в `register_handlers()`

3. **Новая таблица БД:**
   - Создать Pydantic модель в `models/`
   - Добавить CRUD методы в `db/supabase_client.py`

## 🎯 Дорожная карта

- [ ] SMS напоминания (интеграция Twilio)
- [ ] Многоязычная поддержка
- [ ] Админ-панель для управления слотами
- [ ] Отзывы и рейтинги клиентов
- [ ] Повторяющиеся записи
- [ ] Лист ожидания для популярных слотов

## 📊 Оптимизации

Проект включает несколько оптимизаций для повышения производительности:

- ✅ Асинхронный I/O для файловых операций
- ✅ Блокировка файлов для предотвращения гонок
- ✅ Экспоненциальная задержка при опросе очередей
- ✅ Отслеживание изменений файлов
- ✅ Улучшенная обработка ошибок
- ✅ Проверка здоровья процессов
- ✅ Корректное завершение работы
- ✅ Структурированное логирование

См. [OPTIMIZATIONS.md](OPTIMIZATIONS.md) для подробностей.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License — см. файл LICENSE для деталей

## 🆘 Поддержка

По вопросам и проблемам:

- Откройте issue на GitHub
- Контакт: [your-email@example.com]

---

**Построено с использованием:** Python, aiogram 3.x, Supabase, Stripe, Claude AI, APScheduler, Playwright
